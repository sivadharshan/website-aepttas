// basic UI interactions
document.addEventListener('DOMContentLoaded',function(){
  const yearEls = ['year','year2','year3','year4','year5'];
  yearEls.forEach(id => {
    const el = document.getElementById(id);
    if(el) el.textContent = new Date().getFullYear();
  });
  // simple handler for mobile nav toggle(s)
  document.querySelectorAll('.nav-toggle').forEach(btn=>{
    btn.addEventListener('click', ()=>{
      const nav = document.querySelector('.nav');
      if(nav) nav.style.display = (nav.style.display === 'flex') ? 'none' : 'flex';
    });
  });
});

function handleContact(e){
  e.preventDefault();
  // basic demonstration only: wire this to your backend or Netlify Forms / Formspree
  alert('Thanks! Your message was captured in the demo form. Integrate with your backend to process submissions.');
  e.target.reset();
  return false;
}
